package creational.design.patterns.abstractfactory.pattern;

// Defines the Storage interface for storing files in cloud storage systems.
public interface Storage {
	
	 void storeFile(String filename);
	 
}
